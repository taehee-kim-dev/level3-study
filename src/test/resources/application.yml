spring:
  profiles:
    active: test

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.type: trace
  org.apache.coyote.http11: debug
